const fs = require('fs'); const path = require('path'); const assert = require('assert'); const { spawnSync } = require('child_process');
const columns = ['system','repetition','order','resolved_stream','public_type_index_discovery_ms','relevant_stream_discovery_ms','stream_discovery_total_ms','inbox_discovery_ms','subscription_server_discovery_ms','webhook_subscription_creation_ms','notification_subscription_total_ms','discovery_and_subscription_total_ms','subscription_channel','success','error'];
function run(){ const env={...process.env}; delete env.BENCHMARK_POD_URL; const r=spawnSync(process.execPath,['src/benchmark-initialization-preflight.js'],{env,cwd:path.resolve(__dirname,'..'),encoding:'utf8'}); assert.equal(r.status,1); assert.match(r.stderr,/BENCHMARK_POD_URL/); assert.equal(columns.length,15); const source=fs.readFileSync(path.resolve(__dirname,'../src/benchmark-initialization-latency.js'),'utf8'); assert.match(source,/BENCHMARK_WARMUPS\|\|3/); assert.match(source,/BENCHMARK_REPETITIONS\|\|30/); assert.match(source,/BENCHMARK_POD_URL/); console.log('initialization benchmark tests: PASS'); }
if(require.main===module) run(); module.exports={run};
