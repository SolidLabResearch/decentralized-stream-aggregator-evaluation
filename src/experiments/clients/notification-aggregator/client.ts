import * as fs from "fs";
import * as path from "path";
import { WebSocket } from "ws";
import { LDESinLDP, LDPCommunication } from "@treecg/versionawareldesinldp";
import { RDFStream, RSPEngine, RSPQLParser } from "rsp-js";
import { loadExperimentConfig } from "../../config/config";
import { buildActivityIndexQuery } from "../../config/query";
import { monitorCurrentProcess } from "../../monitoring/process-monitor";
import { clientRuntime } from "../shared/runtime";
import { MAX_OUT_OF_ORDERNESS_MS, RawInstrumentation, sha256 } from "../shared/instrumentation";

const N3 = require("n3"); const parser = new N3.Parser(); const config = loadExperimentConfig(); const { clientIndex, outputDirectory, runId } = clientRuntime(); const query = buildActivityIndexQuery(config.streams); const queryId = sha256(query);
const raw = new RawInstrumentation(outputDirectory, { runId, approach: "notification-aggregator", clientId: String(clientIndex), queryId }); const monitor = monitorCurrentProcess(path.join(outputDirectory, `client-${clientIndex}-resource.csv`), config.experiment.resourceSamplingIntervalMs); const results = fs.createWriteStream(path.join(outputDirectory, `client-${clientIndex}-results.csv`), { flags: "w" }); results.write("timestamp,result\n"); const sockets: WebSocket[] = [];
async function run(): Promise<void> { const engine = new RSPEngine(query, { max_delay: MAX_OUT_OF_ORDERNESS_MS, metrics: { run_id: runId, approach: "notification-aggregator", client_id: String(clientIndex), query_id: queryId }, onMetric: (event, metric) => raw.rspMetric(event, metric) }); const emitter = engine.register(); const streams = new RSPQLParser().parse(query).s2r.map((stream: { stream_name: string }) => stream.stream_name); emitter.on("RStream", (event: any) => { for (const item of event.bindings.values()) results.write(`${Date.now()},${JSON.stringify(item.value)}\n`); }); for (const name of streams) { const ldes = new LDESinLDP(name, new LDPCommunication()); const metadata = await raw.measure("stream_discovery", { streamId: name }, () => ldes.readMetadata()); const bucketStrategy = metadata.getQuads(name + "#BucketizeStrategy", "https://w3id.org/tree#path", null, null)[0].object.value; subscribe(engine.getStream(name) as RDFStream, bucketStrategy); } }
function subscribe(stream: RDFStream, bucketStrategy: string): void { const socket = new WebSocket(config.urls.notificationAggregator, "solid-stream-notifications-aggregator", { perMessageDeflate: false }); sockets.push(socket); socket.once("open", () => { const start = raw.now(); socket.send(JSON.stringify({ subscribe: [stream.name] })); const end = raw.now(); raw.write({ operation: "stream_subscription", streamId: stream.name, startEpochMs: start.epochMs, endEpochMs: end.epochMs, startMonotonicNs: start.monotonicNs, endMonotonicNs: end.monotonicNs }); }); socket.on("message", (data) => { try { const message = JSON.parse(data.toString()); const eventId = message.object || message.id || message.eventUrl; const start = raw.now(); const store = new N3.Store(); parser.parse(message.event, (error: Error | null, quad: any) => { if (error) throw error; if (quad) store.addQuad(quad); }); const value = store.getQuads(null, bucketStrategy, null, null)[0].object.value; const end = raw.now(); raw.write({ operation: "parsing_timestamp_extraction", eventId, streamId: stream.name, startEpochMs: start.epochMs, endEpochMs: end.epochMs, startMonotonicNs: start.monotonicNs, endMonotonicNs: end.monotonicNs }); for (const quad of store.getQuads(null, null, null, null)) stream.add(quad, Date.parse(value), eventId); } catch (error) { console.error(`Notification client ${clientIndex}: ${(error as Error).message}`); } }); socket.on("error", (error) => console.error(`Notification client ${clientIndex}: ${error.message}`)); }
const shutdown = async (exitCode = 0) => { sockets.forEach((socket) => socket.close()); await monitor.stop(); await raw.close(); results.end(); process.exit(exitCode); }; process.once("SIGINT", shutdown); process.once("SIGTERM", shutdown); run().catch((error) => { console.error(error); void shutdown(1); });
